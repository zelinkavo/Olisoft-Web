---
interface Props {
  src: string;
  alt: string;
  size?: number;
}

const { src, alt, size = 48 } = Astro.props;
---

<img
  src={src}
  alt={alt}
  width={size}
  height={size}
  loading="lazy"
  decoding="async"
/>
