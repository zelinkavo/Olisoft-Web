---
import PageSimpleCryptoAlerts from '../components/pages/PageSimpleCryptoAlerts.astro';

const lang: 'es' | 'en' = Astro.url.pathname.startsWith('/en') ? 'en' : 'es';
---

<PageSimpleCryptoAlerts lang={lang} />
